var jwt    = require('jsonwebtoken'); // used to create, sign, and verify tokens
var express= require('express');

module.exports = function(app, Contest) {
	app.get('/newContest', function(req, res) {
	});
	
}